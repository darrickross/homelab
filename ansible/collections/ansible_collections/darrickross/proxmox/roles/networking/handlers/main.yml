# handlers/main.yml
---
# Handler: safer than a hard restart on Proxmox/ifupdown2
- name: Reload networking with ifreload
  listen: Reload networking
  become: true
  ansible.builtin.command:
    cmd: ifreload -a
  changed_when: false
