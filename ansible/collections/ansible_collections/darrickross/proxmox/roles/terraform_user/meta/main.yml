---
galaxy_info:
  author: darrickross
  description: >
    Create a Terraform PVE user, custom role, ACL assignment, and API token.
    Stores the generated token secret in Bitwarden via the bw CLI on the
    Ansible controller. Requires BW_SESSION to be set in the environment.
  license: MIT

  min_ansible_version: "2.9"

  namespace: darrickross

  platforms:
    - name: Debian
      versions:
        - bookworm # Debian 12 / Proxmox VE 8

dependencies: []
